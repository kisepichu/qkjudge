type: runtime

work_dir: "."
use_mariadb: true
startup: |
    chmod 777 ./target/release/comproblems
entrypoint: ./target/release/comproblems
http_proxy: 8080
https: on

branch:
    master:
    dev:
      cname: dev_tqk_comproblems.trap.games
